name: hola-mundo

on: [push]

jobs:
  hola-mundo:
    runs-on: ubuntu-lastest
      
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Run dev
        run: run start:Dev             